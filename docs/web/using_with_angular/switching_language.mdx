---
id: switching_language
title: Switching languages
sidebar_label: Switching languages
slug: /web/using_with_angular/switching_languages
---

For language management, use `TranslationService`.

```ts title="language.component.ts"
import { Component } from '@angular/core';
import { TranslateService } from '@tolgee/ngx';

@Component({
  ...
})
export class AppComponent {
  constructor(public translateService: TranslateService) {}
  ...
}
```

Then in template use methods `setLang` and `getCurrentLang`.

```html title="language.component.html"
<select
  [value]="translateService.getCurrentLang()"
  (change)="translateService.setLang($event.target.value)"
>
  <option value="en">🇬🇧 English</option>
  <option value="cs">🇨🇿 Česky</option>
</select>
```