import React from "react";

export const PricingDescription = () => {
  return (
    <p>
      Currently, no limits are applied to translations count. We plan to release
      pricing during July.
    </p>
  );
};
