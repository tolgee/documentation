#### T function also supports additional options and variables
```ts
t('key', 'default value', { <options or variable parameters> })
```

Read more in the [API section](/js-sdk/api/core_package/tolgee#t).