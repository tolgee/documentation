---
id: translating
title: Translating (React)
sidebar_label: Translating
description: 'Learn about several ways how to translate a text using Tolgee React integration: T-component or useTranslate hook.'
---

import MessageFormat from '../../shared/_MessageFormat.mdx';
import TFunctionHint from '../../shared/_TFunctionHint.mdx';


There are several ways how to translate a text using our React integration.

## `useTranslate` hook

Other option how to translate is using of [`useTranslate`](./api#hook-usetranslate) hook. You can use it this way:

```js
import { useTranslate } from '@tolgee/react';

function Component() {
  const { t } = useTranslate();

  return <div title={t('key_to_translate')}>...</div>;
}
```

<TFunctionHint />

### Using namespaces

`useTranslate` hook can be also used for loading additional namespaces.

```ts
const { t, isLoading } = useTranslate('common');
```

This example will add `common` to [active namespaces](/api/core_package/tolgee.mdx#addactivens). You can then also use `isLoading` property, which will tell you if your data are ready.

## The `T` component

Another way is to use [`T` component](./api#t-component)

```jsx
import { T } from "@tolgee/react";

...

<T keyName="translation_key" defaultValue="Default value" params={...} />
```

## Message format

<MessageFormat />
