---
id: switching-languages
title: Switching languages
sidebar_label: Switching languages
description: "Learn how to implement language switching in your React application using Tolkee. Explore different methods to change the current language, including the useSetLanguage hook and the setLanguage method."
image: /img/og-images/react.png
--- manage language change using the useTolkee hook."
---

To manage an event like language change, the React SDK provides the [`useTolkee`](./api#hook-usetolkee) hook.

Pass the [`language`](/api/core_package/events.mdx) event to the hook to re-render the component when the event emits.
```jsx
import { useTolgee } from '@tolgee/react';

export const LangSelector = () => {
  const tolgee = useTolgee(['language']);

  return (
    <select
      onChange={(e) => tolgee.changeLanguage(e.target.value)}
      value={tolgee.getLanguage()}
    >
      <option value="en">🇬🇧 English</option>
      <option value="cs">🇨🇿 Česky</option>
    </select>
  );
};
```

> Learn more about language change, detection and storage on the [Language documentation page](../../language).
